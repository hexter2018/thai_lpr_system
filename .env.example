# Copy to .env if you want to run locally (non-docker) or override.
DATABASE_URL=postgresql+psycopg2://alpr:alpr@localhost:5432/alpr
REDIS_URL=redis://localhost:6379/0
STORAGE_DIR=./storage
MODEL_PATH=./models/best.pt
MASTER_CONF_THRESHOLD=0.95
CORS_ORIGINS=http://localhost:5173
# Frontend (Vite) - optional override for API base (default uses same origin / proxy)
VITE_API_BASE=http://localhost:8000
